namespace Director.Primitives
{
    /// <summary>
    /// Identifiers for cast member fields used by the Lingo 'the' syntax.
    /// This is only a minimal subset required by the current C# port.
    /// </summary>
    public enum CastField
    {
        BackColor,
        CastLibNum,
        CastType,
        FileName,
        ForeColor,
        Height,
        Hilite,
        Loaded,
        Modified,
        MemberNum,
        Name,
        Number,
        Rect,
        PurgePriority,
        ScriptText,
        Size,
        Type,
        Width
    }
}
